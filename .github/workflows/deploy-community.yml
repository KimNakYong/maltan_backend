name: Deploy Community Service

on:
  push:
    branches:
      - main
    paths:
      - 'backend/community-service/**'
      - '.github/workflows/deploy-community.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Build Community Service
        working-directory: backend/community-service
        run: |
          mvn clean package -DskipTests
      
      - name: Build Docker Image
        working-directory: backend/community-service
        run: |
          docker build -t community-service:latest .
      
      - name: Stop Old Container
        run: |
          docker stop community-service || true
          docker rm community-service || true
      
      - name: Run New Container
        run: |
          docker run -d \
            --name community-service \
            --network docker_maltan-network \
            --restart unless-stopped \
            -p 8083:8083 \
            -e DATABASE_URL=jdbc:postgresql://10.0.2.15:5432/community_db \
            -e DATABASE_USERNAME=maltan_user \
            -e DATABASE_PASSWORD=maltan_pass123 \
            -e REDIS_HOST=10.0.2.15 \
            -e REDIS_PORT=6379 \
            -e SERVER_PORT=8083 \
            -e USER_SERVICE_URL=http://user-service:8081 \
            community-service:latest
      
      - name: Health Check
        run: |
          sleep 10
          docker ps | grep community-service
          docker logs --tail 50 community-service
          curl -f http://localhost:8083/actuator/health || echo "Health check pending"
